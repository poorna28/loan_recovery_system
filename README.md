# Loan Management System

A full-stack application to manage loan applications, repayments, and recovery processes.

## Project Structure

- `client/`: React frontend
- `server/`: Node.js + Express backend
- `database/`: SQL schema and seed data

## How to Run

### Frontend
```bash
cd client
npm install
npm start
